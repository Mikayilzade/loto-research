# H219 audit append

| ID | Lottery mechanism | Test / method | Result | Status |
|---|---|---|---|---|
| H219 | RI Keno H175 restricted hybrid | Exact 4,878-cut screen of all `36 x 3,992 = 143,712` H212 normalized classes, computationally reformulated from H217 Python bigint threshold masks to direct NumPy incidence matrices | Predicate is exactly equivalent; new 36-shard workflow added. H218 merged result was still absent at packet start, so no survivor/closure claim is made yet. | **INCONCLUSIVE — exact merged H219 output pending**; `research/h219_vectorized_exact_h175_shards.md`, `src/loto_research/h219_vectorized_bc_shards.py` |

Do not treat workflow timeout/missing output as evidence for universal `n3>=3`. If H219 produces survivors, exact `n3<=2` separation is mandatory on each survivor before any validation claim.
