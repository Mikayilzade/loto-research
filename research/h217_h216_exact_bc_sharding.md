# H217 — exact H216 B/C sharding

Status: INCONCLUSIVE / COMPUTATION UNBLOCKING.

H216's expected survivor file remained absent. H217 does not alter the mathematical test and does not repeat a closed experiment. It decomposes the exact H216/H213 necessary-cut screen into the 36 normalized B/C coefficient pairs. Each shard checks all 3,992 H212 A-orbit representatives against the same 4,878 exact H185+H186 rows and rejects exactly when any row gives `n3 < 3`.

This preserves the exact restricted-family proof logic while replacing one monolithic run by 36 independently observable jobs. A zero survivor union closes the H212/H214 restricted diagonal family under the existing exact witness bank. Any nonzero survivor remains unresolved and must undergo exact `n3<=2` separation; surviving the bank is not validation.

No SUCCESS or restricted-family closure is claimed in H217 itself because shard outputs are not yet committed/collected.
