# H189 — S3 coefficient-orbit canonicalization for the H175 restricted master

Updated: 2026-08-22
Status: **NO SUCCESS; EXACT FURTHER REDUCTION OF RESTRICTED MASTER**

## Target
Strengthen H188 without repeating any closed separator test. The unresolved H175 4,336-play doubled 3-spot hybrid still requires universal `n3>=3` on every balanced `4+4+4+4+4` draw.

H188 canonicalized the three single-support layers B=`(0,3,4)`, C=`(1,3,4)`, D=`(2,3,4)` to D coefficient/shift `(1,0)`, B/C zero shifts and ordered coefficients `a_B<=a_C`. That left 36 ordered coefficient pairs `(a_B,a_C)` over the eight odd units modulo 16.

## New symmetry
The supports B,C,D are structurally symmetric: any permutation of groups `0,1,2` permutes B,C,D while preserving the three-layer support A=`(0,1,2)` as a set. Therefore the full `S3` permutation group acts on the coefficient triple `(a_B,a_C,a_D)`.

Independently, the H187 scaling symmetry multiplies all three B/C/D coefficients by the same unit `r` modulo 16 before one chosen coefficient is normalized to 1. Consequently coefficient triples are equivalent under:

1. arbitrary permutation of the three entries;
2. multiplication of all three entries by any odd unit modulo 16.

After choosing any permuted third entry as D and multiplying by its inverse, D becomes 1 again. The remaining B,C pair can be sorted. Thus H188's 36 pairs are not 36 distinct symmetry classes.

## Exact orbit enumeration
Enumerating all H188 pairs `(b,c)` with `b<=c`, treating the triple `(b,c,1)` under all six permutations and re-normalizing the third entry to 1, yields exactly **15 coefficient orbits**.

Canonical representatives:

`(1,1), (1,3), (1,5), (1,7), (1,9), (1,11), (1,13), (1,15), (3,5), (3,7), (3,9), (3,13), (3,15), (5,9), (7,9)`.

The explicit mapping of all 36 H188 pairs to these 15 representatives is persisted in `data/derived/h189_s3_coefficient_orbits.json` and reproducibly generated by `src/loto_research/h189_s3_coefficient_orbits.py`.

## Representative-count consequence
A still consists of any 3 distinct diagonal cyclic-affine parameters out of 128, so there are

`C(128,3) = 341,376`

A choices. Multiplying by the 15 exact B/C/D coefficient orbits gives

`341,376 * 15 = 5,120,640`

canonical restricted designs.

This is:
- **2.4x smaller than H188's 12,289,536** representatives;
- about **550.4x smaller than H187's 2,818,400,256** representatives;
- about **139,808x smaller than H180's 715,917,361,152 raw restricted designs**.

## Proof discipline
This is a WLOG symmetry quotient only. It does **not** prove the diagonal family infeasible and does **not** validate any 4,336-play construction. All prior balanced witness cuts remain valid because coordinate relabelings bijectively map balanced draws to balanced draws and preserve the universal `n3>=3` property.

## Next action
Use the full merged H185+H186 cut bank with an H189 master restricted to these 15 canonical coefficient representatives. If feasible, exact-separate returned candidates; if solver-certified infeasible, the entire restricted diagonal cyclic-affine family is closed. Timeout/no incumbent remains inconclusive.

## Result
**ЕЩЁ НЕ УСПЕХ.** H189 reduces the exact restricted representative space to 5,120,640 designs, but no universal `n3>=3` construction or impossibility proof is established yet.
