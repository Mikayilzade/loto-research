# CHECKED PROJECTS AND TESTS — H284 APPEND

## H284 — Virginia Pick 3 + advertised 50% first-deposit match

Checked 2026-08-26.

Result: **REJECTED / CLOSED for this mechanism.**

The current Virginia Pick 3 fixed-pay table has a maximum exact average gross ratio of **50%** across the standard base primitive classes (Exact, Pair, Combo), with Any Order at 48% and 50/50 at 49%. A deterministic 50% playable-balance match scales cash deposit to at most 1.5x wagerable balance, so any nonnegative additive base portfolio has `minimum gross <= average gross <= 0.75 × original deposit`.

A player-favorable FIREBALL stress granting the add-on a 57% standalone gross ceiling still yields at most 53.5% on an equal-stake base+add-on blend and 80.25% cash recovery after the 50% subsidy.

Do not repeat ordinary Virginia Pick 3 + 50%-match cover searches unless the subsidy exceeds the >100% match hurdle, the paytable changes materially, or a separate deterministic cash-equivalent reward is added.
