# H191 — exact A-set stabilizer classification after H190

Updated: 2026-08-22
Status: **NO SUCCESS; symmetry route materially exhausted beyond H188**
Scope: lottery-only continuation of H175/H188/H190.

## Target
H190 withdrew H189's global full-S3 quotient because most restricted diagonal A-layers

`x2 = a*x0 + a*x1 + c (mod 16)`, odd `a`,

are not preserved when the output coordinate is swapped with an input.

H191 asks the narrower exact question required by STATUS: for which actual three-layer A-sets is a larger coordinate stabilizer valid?

## Exact layer audit
For each of all 128 restricted A-layers and all 6 permutations of coordinates `(0,1,2)`, H191 compares the exact 256 triples after permutation against the complete restricted 128-layer family.

Result:
- identity `(0,1,2)`: 128/128 preserved;
- input swap `(1,0,2)`: 128/128 preserved;
- each of the other four coordinate permutations: exactly 16/128 preserved;
- those 16 are precisely slope `a=15`, all shifts `c=0..15`.

For `a=15`, the layer equation is

`x0 + x1 + x2 = c (mod16)`,

which is fully symmetric in the three coordinates. Every element of S3 therefore fixes each individual `(15,c)` layer setwise.

## Exact A-set stabilizer theorem
Let A be any three distinct restricted diagonal layers.

1. Every A-set has the order-2 stabilizer generated by swapping the two input coordinates `(0,1)`; this fixes every diagonal layer individually.
2. A-set has any stabilizer element outside that order-2 subgroup **iff every one of its three layers has slope `a=15`**.
3. In that exceptional case each layer is individually S3-invariant, so the entire A-set has full stabilizer S3.

Thus there are only two stabilizer types:
- generic C2: all A-sets except all-`a=15` sets;
- exceptional S3: choose any 3 shifts among the 16 `a=15` layers.

Counts:
- all A-sets: `C(128,3) = 341,376`;
- exceptional full-S3 A-sets: `C(16,3) = 560`;
- generic C2 A-sets: `340,816`.

## Consequence for H188 representative count
H188 has 36 normalized B/C coefficient-pair choices per A-set.
H189's 15-orbit full-S3 quotient is valid only for the 560 exceptional A-sets.

Therefore a safe stabilizer-aware count is

`340,816*36 + 560*15 = 12,277,776` representatives.

Compared with H188's globally valid

`341,376*36 = 12,289,536`,

the exact saving is only

`11,760` representatives = `0.09578%`.

So the hoped-for symmetry acceleration is negligible. This closes the current stabilizer-quotient optimization direction: more material progress must come from stronger valid cuts, separator improvements, direct enumeration/pruning, or a broader structural theorem.

## Reproducibility
Code: `src/loto_research/h191_a_set_stabilizer_classification.py`
Data summary: `data/derived/h191_a_set_stabilizer_summary.json`

## Result
**ЕЩЁ НЕ УСПЕХ.** H191 proves exactly where full S3 is legal and shows it reduces H188 by only ~0.096%; the universal H175 `n3>=3` gate remains unresolved.